# 相遇签到系统 · 本地验收

验证日期：2026-09-22。正式数据目录与测试目录分离，以下名单均为人为构造的测试资料。

## 已完成

- `npm run build`：TypeScript 检查、Vite 生产构建通过。
- `npm test`：6 组测试通过，包括真实 XLSX、DOCX 表格、DOCX 正文、文字 PDF、CSV 和端到端接口链路。
- `npm audit`：0 个已知漏洞。
- 数据库重启后，名单、活动设置、二维码凭证和首次签到时间保留。
- 未登录不能查看参会资料或操作导入、签到；跨来源写请求被拒绝。

## 浏览器实际操作

在 Codex 浏览器内完成，检查了桌面和手机宽度的页面：

1. 使用独立测试账号登录主办方后台。
2. 从文件选择器上传 Excel，预览正确识别 3 人的手机号、姓名、公司、职位、身份，确认后保存。
3. 从文件选择器上传 Word 表格，正确新增 1 人，导入记录显示中文文件名。
4. 从文件选择器上传文字 PDF，正确保留 phone / Name / Company 对应关系，新增 1 人。
5. 参会者输入未登记手机号，页面提示未找到资料；输入已登记手机号，生成真实二维码并显示脱敏手机号。
6. 将上述页面实际生成的二维码图片，通过主办方“从相册识别”上传；成功查回同一位来宾“张晓”，资料包含“相遇科技 / 产品经理 / 嘉宾”。
7. 点击“核对无误，确认签到”，界面显示首次签到时间。再按手机号查找时仍显示已签到，没有重复确认按钮。
8. 名单搜索手机号后只显示目标来宾，状态为已签到；概览显示 5 位登记、1 位签到、4 位待签到、20% 进度。
9. 修改活动名称、日期、时间、地点、欢迎语并保存；服务端与参会者页面展示新值。
10. 手机界面使用固定底部导航；身份资料显示为底部弹窗，表格在自身容器内横向滚动。

过程中修复：Vite 默认字符串代理改变 Host 导致来源校验拒绝；无正文的 Word 表格产生多余识别提示；手机导航名称重复；日期控件显示值未进入提交内容。已按实际 UI 流程复核。

## 边界

- 已验证真实二维码图片的浏览器解码和后端查回；**未通过实体手机摄像头实拍验收**。
- 手机实时相机需 HTTPS；局域网 HTTP 可使用相册识别、手机号查询以及其他功能。
- PDF 仅处理可提取文字的文件；未实现 OCR。复杂排版需要人工核对预览。
- 不包含短信验证、多人分权限、多活动管理。
- Dockerfile 已提供，当前环境未执行容器构建；未发布到公网。

## 本地证据

- `test-results/测试参会名单.xlsx`
- `test-results/测试参会资料.docx`
- `test-results/test-attendees.pdf`
- `test-results/browser-ticket.png`：仅为隔离测试数据的入场码
- `test-results/mobile-checkin.png`
- `test-results/mobile-dashboard.png`
- `test-results/desktop-dashboard.png`

这些测试文件不写入正式数据库，也不纳入版本控制。

## 交付时运行状态

生产构建通过后已使用 `npm start` 启动本地服务，端口 3001。`/` 与 `/admin` 正常返回页面，`/api/auth/status` 返回 `setupRequired: true`；正式数据库参会人数为 0，等待主办方设置自己的密码并导入真实名单。同 Wi-Fi 地址 `http://192.168.20.79:3001` 已验证可响应。

## Excel 格式兼容修复

用户上传实际交付的 Excel 时出现 `Cannot read properties of undefined (reading 'sheets')`。复现后确认：原 ExcelJS 读取器按不带前缀的 XML 标签名匹配，无法解析该文件的命名空间前缀。仅给普通测试工作簿的 workbook 标签加前缀，即可复现同一异常。

修复：XLSX 读取改用 read-excel-file，保留所有工作表与空白列的对应关系；无法读取的文件返回中文提示。ExcelJS 仅保留为测试数据生成依赖。新增带前缀工作簿 / 工作表和损坏文件回归测试，当前 8 项测试全部通过，构建通过，依赖审计为 0 漏洞。

已重启本地实际服务，并通过浏览器文件选择器上传原交付文件。预览正确显示 1 位参会者，手机号、姓名及其余 6 个字段全部匹配，无识别警告。停留在“确认导入”前，未写入参会名单。今后生成导入文件，应以实际导入程序和网页预览验证交付文件，而不只验证文件内部数值。

## Zeabur 线上验收

2026-09-22，代码发布到 `https://github.com/zxc9802/qiandao`，服务运行于 `https://qiandao-zxc9802.zeabur.app`。Zeabur 自带镜像仓库的拉取持续停滞，经用户要求改为 GitHub Container Registry；新镜像成功启动。通过 GitHub Actions 在 Node.js 24 容器中完成 9 项测试及生产构建。镜像构建阶段显式安装开发依赖，并让测试不使用生产环境中的初始化密码。

- 实际浏览器操作：使用线上管理密码登录，上传仅含 1 位虚构来宾的 Excel，预览后确认导入；在参会者网页输入其手机号生成二维码，将网页实际生成的二维码图片上传到“从相册识别”，正确显示对应身份资料，点击确认签到后展示已签到。
- HTTPS 登录 Cookie 带有 Secure、HttpOnly、SameSite=Strict；未登录访问名单返回 401，重复初始化后台返回 409。公开领码接口只返回随机凭证和脱敏号码。
- 重复签到返回已签到，首次签到时间不变。
- `/app/data` 已核对为实际挂载的持久卷。重启后容器实例名称改变，原登录会话、身份信息、二维码凭证与首次签到时间完全保留。
- 运行容器中的 `server/app.ts` 与 `server/importer.ts` 的 SHA-256 和本地源码一致。
- 线上验收截图与临时凭证保存在忽略目录 `test-results/`；真实用户名单、数据库、密码未提交到仓库。
- 仍未通过实体手机摄像头实拍验收；已验证线上真实二维码图片的浏览器识别。
